package com.ureka.team3.utong_backend.gift.enums;

import lombok.Getter;
import lombok.RequiredArgsConstructor;

@Getter
@RequiredArgsConstructor
public enum GifticonStatus {
    EXPIRED,
    AVAILABLE

//    private final String code;       // DB상의 코드값 (ex: 001)
//    private final String codeName;   // 사람이 보는 이름 (ex: 사용 가능)
}
